extends base
include mixins/video

block content 
    form(method="GET")
        input(placeholder="Search by title", type="text", name="keyword") 
        input(value="Search Now", type="submit") 

    div
        each videoIndex in videos 
            +video(videoIndex)